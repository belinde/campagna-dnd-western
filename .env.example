# Copia in .env (non committato): cp .env.example .env
# Crea un token su https://huggingface.co/settings/tokens
# huggingface_hub legge HF_TOKEN (o HUGGING_FACE_HUB_TOKEN) per download e rate limit.
# Per pyannote: accetta le condizioni su HF per `pyannote/speaker-diarization-community-1`
# (include i sotto-modelli necessari; non serve più accettare `pyannote/segmentation-3.0` a parte).

HF_TOKEN=
