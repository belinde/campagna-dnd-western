# Segreti e ambiente locale
.env
.venv/
venv/
env/
.env.*.local

# Build e artefatti di installazione Python
# La regola `build/` ignora ogni directory chiamata `build` in qualsiasi livello (inclusa tools/build/, output Jekyll).
/tools/build/
build/
dist/
*.egg-info/
*.egg
*.whl

# Bytecode, cache e strumenti Python
__pycache__/
*.py[cod]
*$py.class
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
.hypothesis/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# Registrazioni sessione (file locali voluminosi)
sessione/audio/

# IDE / editor
.idea/
*.swp
*.swo
*~

# Claude Code: impostazioni e permessi locali (la config condivisa resta versionata)
.claude/settings.local.json

# Sistema operativo
.DS_Store
Thumbs.db

# Jupyter (se usato in futuro)
.ipynb_checkpoints/
